services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      ASE_REPOSITORY_ROOT: /workspace
    volumes:
      - ./:/workspace:ro
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

